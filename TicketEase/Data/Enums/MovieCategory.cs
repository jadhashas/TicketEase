namespace TicketEase.Data.Enums
{
    /*
     * Une énumération (enum) :
     * en C# est un type de données qui permet de définir un ensemble de valeurs nommées constantes. Une énumération permet de créer un groupe de valeurs associées, 
     * facilitant ainsi leur utilisation dans le code et rendant le code plus lisible et maintenable.
     */
    public enum MovieCategory
    {
        Action = 1,
        Comedy,
        Drama,
        Documentary,
        Cartoon,
        Horror
    }
}
